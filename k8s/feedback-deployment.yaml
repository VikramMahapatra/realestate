
apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-service
  namespace: realestate
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feedback-service
  template:
    metadata:
      labels:
        app: feedback-service
    spec:
      containers:
        - name: feedback
          image: ghcr.io/<org>/feedback-service:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://user:pass@postgres.realestate.svc.cluster.local:5432/appdb
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: common-secrets
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: feedback-service
  namespace: realestate
spec:
  type: ClusterIP
  selector:
    app: feedback-service
  ports:
    - port: 80
      targetPort: 8000
