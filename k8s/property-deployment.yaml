
apiVersion: apps/v1
kind: Deployment
metadata:
  name: property-service
  namespace: realestate
spec:
  replicas: 1
  selector:
    matchLabels:
      app: property-service
  template:
    metadata:
      labels:
        app: property-service
    spec:
      containers:
        - name: property
          image: ghcr.io/<org>/property-service:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://user:pass@postgres.realestate.svc.cluster.local:5432/appdb
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: common-secrets
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: property-service
  namespace: realestate
spec:
  type: ClusterIP
  selector:
    app: property-service
  ports:
    - port: 80
      targetPort: 8000
